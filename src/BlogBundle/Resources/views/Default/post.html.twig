{% extends "BlogBundle:default:layout.html.twig" %}

{% block title %} {{ post.title }} {% endblock %}

{% block body %}
    <h3>{{ post.author }}</h3>
    <h4>{{ post.title }}</h4>
    <h4>{{ post.body }}</h4>

{% endblock %}